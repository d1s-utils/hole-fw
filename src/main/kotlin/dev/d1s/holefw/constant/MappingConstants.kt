package dev.d1s.holefw.constant

const val GET_AVAILABLE_DIRECTORIES_MAPPING = "/"
const val GET_OBJECTS_BY_GROUP_MAPPING = "/{group}"

// {group} is basically pointless, but it's here for consistency
const val GET_RAW_OBJECT_MAPPING = "/{group}/{id}"